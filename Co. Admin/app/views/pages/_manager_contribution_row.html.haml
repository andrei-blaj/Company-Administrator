- UserProjectRelation.where(:project_id => @project.id).each do |current_relation|
	%tr.odd{:role => "row"}
		- @u = User.find_by(id: current_relation.user_id)
		%td= @u.email
		%td= current_relation.contribution
		%td= current_relation.created_at.to_s